module.exports = {
    // Token bota
    token: "",
    // Kanał z logami (ID)
    log_channel: "",
    // Prefix
    prefix: "+",
    // Timeout pomiędzy odpalaniem kolejki w MS
    timeout: 7200000
}